// need to control state for focused time,

/**
 * reducer actions
 * - jump to time (implicitly unlocks live time)
 * - lock to live time
 * - set zoom
 *
 * state (basically the whole calendar config)
 * - focusedTime
 * - angleToFocus (actually a constant based on total rotations)
 * - zoom
 */

// consider splitting apart your calendar config arguments to be what is actually used
